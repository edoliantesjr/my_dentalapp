import 'package:flutter/material.dart';

class ViewDentalNoteView extends StatelessWidget {
  const ViewDentalNoteView({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
