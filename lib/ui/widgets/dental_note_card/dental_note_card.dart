import 'package:flutter/material.dart';

class DentalNoteCard extends StatelessWidget {
  const DentalNoteCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //todo code for dental note card
    return Container();
  }
}
