import 'package:flutter/material.dart';

class PToothConditionCard extends StatelessWidget {
  const PToothConditionCard({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    //todo: code for patient tooth condition card
    return Container();
  }
}
